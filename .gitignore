node_modules/
.DS_Store
*.pdf

# local database — the directory itself is kept via backend/data/.gitkeep
backend/data/*.db
backend/data/*.db-*

# environment / credentials
.env
backend/.env

# generated credentials, read once then delete
backend/ADMIN-PASSWORD.txt
